import SidebarComponent from '../components/SidebarComponent';
import React from 'react';

const Book = () => {
  return (
    <div style={{ height: '100%', backgroundColor: '#ecf0f1' }}>
      <SidebarComponent />
    </div>
  );
};

export default Book;
